kind : Pod
apiVersion: v1
metadata: 
  name: tolerations3
  labels:
    purpose: "tolerations_demo"
spec:
  nodeSelector:
    topology.kubernetes.io/zone: us-east-1c
  containers:
  - name: nginx
    image: nginx