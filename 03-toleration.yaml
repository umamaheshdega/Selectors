kind : Pod
apiVersion: v1
metadata: 
  name: tolerations1
  labels:
    purpose: "tolerations1_demo"
spec:
  nodeSelector:
    topology.kubernetes.io/zone: us-east-1c
  containers:
  - name: nginx
    image: nginx